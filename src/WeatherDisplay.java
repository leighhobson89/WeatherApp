public interface WeatherDisplay {

    /**
     * Retrieves and displays the current weather for London from the OpenWeatherMap API.
     */
    void displayCurrentWeatherForCity(String userInput);

}